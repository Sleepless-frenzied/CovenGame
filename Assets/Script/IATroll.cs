/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace classEnemy
{
public class  IA_Troll : MonoBehaviour
{
    Animator animator;
    CapsuleCollider playerCollider;
    public GameObject itself;
    //Troll troll = this.gameObject.AddComponent<Troll>();
    // Update is called once per frame
    void Update()
    {
        if (Vector3.Distance(troll.transform.position,troll.target.transform.position)>troll.fightingRange)
        {
            troll.chase();
        }
        if (Vector3.Distance(troll.transform.position,troll.target.transform.position)<=troll.attackRange)
        {
            troll.attack();
        }
        else
        {
            troll.fighting();
        }
    }
}
}*/